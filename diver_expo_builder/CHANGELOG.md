# Changelog

## 0.1.0

- Initial public release.
- CLI that aggregates Expo Router routes into `diver/app_urls.json`.
- `defineDiverRoute` metadata helper exported from the `./route` subpath.
- `upload` command that POSTs the generated JSON to the Diver API.
